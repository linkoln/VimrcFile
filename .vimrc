"lin vim配置文件

set hlsearch					"高亮度反白

set backspace=2					"可随时用退格键删除,0或1则不行

set autoindent					"自动缩进

set ruler					"可显示最后一行的状态

set showmode					"左下角那一行的状态

set nu						"显示行号

set bg=dark					"显示背景色,还可以设置 light,etc

syntax on					"进行语法检验,颜色显示

set shiftwidth=4				"设置tab为4个空格
